import React from 'react';

import { Container } from 'src/components/common/container/AppContainer.styles';

const AppContainer = ({ children }) => <Container>{children}</Container>;

export default AppContainer;
