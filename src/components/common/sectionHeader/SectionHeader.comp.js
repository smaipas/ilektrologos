import React from 'react';

import { SectionH1 } from 'src/components/common/sectionHeader/SectionHeader.styles';

const SectionHeader = ({ children }) => <SectionH1>{children}</SectionH1>;

export default SectionHeader;
